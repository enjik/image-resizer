const resize = require('../resize.js');

describe('resize.js', function() {
  it('should return a resized image with specified width and height');
  it('should return a resized image with specified width');
  it('should return a resized image with specified height');
  it('should return the original image if no dimensions specified');
});
